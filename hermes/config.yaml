# Hermes Agent configuration (seeded into $HERMES_HOME/config.yaml at runtime).
# Memory provider: Honcho (self-hosted). The Honcho connection itself comes
# from the HONCHO_BASE_URL / HONCHO_API_KEY environment variables.
memory:
  provider: honcho
